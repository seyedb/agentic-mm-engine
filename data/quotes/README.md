# Quote Datasets

This directory contains small public top-of-book quote captures used by the research pipeline.

The files are reproducibility fixtures, not private trading data. New local captures should be reviewed before committing so the checked-in dataset set stays small and intentional.
