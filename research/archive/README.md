# Archived Research Helpers

These scripts are historical diagnostics and tuning helpers. They are kept for reference, but they are not part of the current main workflow.

Use the top-level scripts in `research/` for the reproducible project path.

The current contextual-bandit trainer reuses helper functions from `train_bandit_selector.py`, but the old script itself remains a diagnostic rather than a promoted workflow step.
